services:
  - type: web
    name: youtube
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py --host 0.0.0.0 --port 5000
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"